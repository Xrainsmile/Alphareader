# 2026-07-14

## 部署记录
- 提交并推送 `6db3fb2` 到 GitHub（main）：评分 LLM 切换 DeepSeek-V4（stream_chat 封装）、首页 TOP5 右侧栏空白修复（`height:100vh`→`max-height:100vh`+`align-self:flex-start`）、favicon 换为 icon.png。
- 部署到 Lighthouse 43.136.86.36（`/home/Alphareader`）：`git pull --ff-only` + alembic（无新迁移，no-op）+ `docker compose up -d --build`。
- 验证：后端 health=200；icon.png HTTPS=200；线上 index.html 已引用 `/icon.png`。
- 注：首页右侧栏空白根因是 `.pc-right-panel` 在 ≥1200px 断点被强制 `height:100vh`，内容仅 5 条 TOP5 新闻，下方大片空白。

## PC 三栏布局统一化（2026-07-14）
- 抽出共享组件 `frontend/src/components/common/PcSidebar.vue`（左导航，≥768px sticky 显示，active 高亮，uni.switchTab 跳转 4 个 tabBar 页）。
- 抽出共享样式 `frontend/src/styles/pc-layout.css`（`.page-layout`/`.container`/`.pc-right-panel` + 媒体查询：移动单列、≥768px 左导航+内容、≥1200px +右看板 240px sticky），在 `main.js` 全局 import。
- Stocks/Reports/SEPA 三页重构为与 News 一致的三栏外壳（`page-layout` > PcSidebar + container + pc-right-panel），并各加右看板内容：Stocks=策略速览（点击切 tab）、Reports=栏目导航（点击切 tab）、SEPA=账户速览（复用已有 account 数据）。
- 各页原 `.xxx-container` 的背景/宽度覆盖已移除，改由共享 `.container`（max-width 700/640，灰底）；SEPA 内卡片 `margin 0 24rpx` 改为 `0` 避免与 container 内边距叠加。
- News 页保持原样（其布局 CSS 仍内联在 index.vue scoped，功能等价）。
- 提交 `460bed2` 并部署，验证 `pc-right-panel`/`page-layout` 已打入构建 CSS。
