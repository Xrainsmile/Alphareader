# 2026-07-14

## VCP 投资策略页改版 收尾（阶段一+二）
- 提交 `2498d85`：指数采集改腾讯财经优先（CN `sh{code}` day 键，A股 281 条真实数据）+ `_save` 修复 trade_date 字符串→date + `load_benchmark_index` 支持 primary+alternatives 回退。已 `git push` 并部署服务器。
- 关键坑：服务器 `docker compose build web` 跑的是**旧工作树代码**（未 pull 到 `2498d85`），导致 `--no-cache` 重建后仍报旧 `_save` 错。修正：先 `git pull --ff-only` 再 `--no-cache` build + `--force-recreate`。验证容器代码：`docker exec alpha-web grep 'trade_date=_td' /app/app/services/vcp_suitability.py`。
- API 验证：`docker exec alpha-web curl -s http://localhost:8000/api/v1/strategy/overview?market=CN -H 'X-API-Key: ...'`（Nginx 外走 HTTPS，容器内走 8000）。
- 强制重算：`compute_vcp_adaptability(market, date.today(), save=True)` 覆盖缓存。CN 五维均为真实数据；US 受腾讯美股仅返回 1 天 + yfinance 限流，走合成代理 + DATA_DELAY。
- 已更新 MEMORY.md（部署缓存坑 / Nginx 301 / API 鉴权 / VCP 章节）。

## 美股数据源接入（用户："尝试接入其他美股数据源"）
- 实测服务器可达性：Eastmoney HTTP 000（不可达）、Stooq 反爬 JS 验证、Nasdaq API symbol not exists、Sina `US_MinKService` **可用且完整历史**（2004 起，~287 条/420天）。腾讯美股 fqkline 仅返 1 天（确认不可用）。
- 提交 `5994472`：新增 `_fetch_us_sina`（`.INX→^GSPC`、`.IXIC→^IXIC`，入库 index_code 对齐 VCP），`fetch_indices` US 分支优先新浪、yfinance/腾讯降级。已 push + 部署（pull + --no-cache build + force-recreate）。
- 效果：US 适配度从「全合成+DATA_DELAY」升级为「大盘趋势 25/25 + 波动环境 20/20 真实，中性 63 分」；`load_benchmark_index` 真实指数门槛(≥250) 现满足。
- **残余限制（已闭环）**：US 市场宽度/突破/活跃度此前"数据不足"+DATA_DELAY，根因有二：(a) VCP `target_date=today` 与美股实际最新交易日（T+1 时差）精确不匹配；(b) US 个股行情陈旧（仅到 07-10）。两者均已修复（见下条）。
- 清理 `market_adaptability` 脏行（`market=''`、`market='CN\'`，测试转义遗留）。
- 测试坑：ssh 外包本地双引号时 `$M`/`$K` 被本地 shell 提前展开成空串，致 overview 收到 `market=`；须用 `\$M`/`\$K`。

## US 个股行情接入 + VCP 目标日期回退（用户："好的" → 接续上轮）
- 提交 `b429e31`：`us_data_fetcher.py` 新增 `_sync_fetch_sina_us_kline`（`US_MinKService.getDailyK?symbol={ticker}`，`BRK-B→BRK.B`），全量/增量下载均改**新浪优先、腾讯兜底、yfinance 最后兜底**；`vcp_suitability.py` 新增 `_resolve_market_date`，`compute`/`get` 入口将目标日期按市场回退到「该市场已有数据的最新交易日」（根治 T+1 时差）。已 push + 部署（pull + --no-cache build + force-recreate）。
- 关键发现（推翻之前假设）：US 个股数据**一直存在**（最新到 07-10、482 只），VCP US 三项"数据不足"并非缺数据，而是 `target_date=today(07-14)` 与 US 实际交易日(07-13)精确不匹配；在 07-10 当日三项可正常算出（breadth 63.69 / turnover 65.5 / breakout 284 样本）。仅刷新+T+1 回退即解决。
- 服务器全量刷新（detached 容器内跑一次性脚本，Sina 主源）：S&P500 503 只、160660 条、2025-07-14~2026-07-13，Sina 成功率 ~100%（499/503）。
- 终验：US VCP **五维全真实、无 DATA_DELAY**，favorable 83（大盘25/宽度20/波动20/突破18/活跃度0~7）；CN 回归正常（07-14、cautious 13、全真实）。`get_vcp_adaptability('US', None)` 解析到 trade_date=2026-07-13。
- 坑：① `POST /api/v1/stocks/us/update_quotes` 返回 405（未深究，改用容器内 detached 脚本刷新）；② 容器内 `python script.py` 因 sys.path[0]=脚本目录找不到 `app` 包 → 须 `PYTHONPATH=/app`；③ `docker exec -d` 日志在容器内 `/tmp`，宿主机查不到；④ 宿主机 `rm` 在 `/home/Alphareader` 挂载路径偶发 300s 超时（无害，临时脚本未删干净，下次重建即失）。

## 待办 / 已知
- 服务器 `backend/scripts/_tmp_check.py` 残留（SSH rm 超时未删，无害，下次 git checkout 清除）。
- 前端面板浏览器实际渲染未人工验证（仅后端 API 已验证）。
- 腾讯美股 fqkline 对**个股** `.OQ` 可用（320 天），仅**指数**返 1 天——故个股兜底用腾讯、指数必须用新浪。
