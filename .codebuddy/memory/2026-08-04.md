# 2026-08-04

## Stocks / SEPA 对外隐藏 + 侧门实现（前端）
- 需求：对外隐藏 Stocks 与 SEPA 入口；在 Reports 旁加「!」按钮（带动画），连续点 3 次弹出密码框，密码复用 Stocks 自身密码，正确后显示 Stocks/SEPA。
- 改动文件：
  - 新增 `frontend/src/utils/useGate.js`：模块级单例 `isOpen` ref + `openGate({token})`（写 gate_open + sb_token/sb_unlocked/sepa_token/sepa_unlocked）+ `checkGate`。
  - 新增 `frontend/src/components/common/GateButton.vue`：圆形「!」按钮，待机呼吸动画 + 点击摇摆动画（本地 keyframes，未 npm 安装）；3 次连点（1.5s 内）弹 `SandboxPasswordModal`；`verifySandboxAccess` 通过则 `openGate`，失败置 error。
  - `PcSidebar.vue`：Stocks/SEPA 改 `v-show="isOpen"`，Reports 下加 GateButton；navTo 对隐藏页用 `navigateTo`（已从原生 tabBar 移除）。
  - `pages.json`：tabBar.list 移除 stocks/sepa（保留 page 定义，deep-link 仍可用）。
  - `reports/index.vue`：页头（移动端）加 GateButton + 解锁后 Stocks/SEPA 入口 chip（navigateTo）；桌面端经媒体查询隐藏这些移动端元素（由侧栏承载）。
- 安全：后端 verify-access 错误密码返回 403，前端 catch 不写 gate_open，无误开。
- 未提交（用户本次未要求 commit）。lint 全过；前端在 Docker 部署时构建（本地无 node_modules 未本地 build）。

## 事件记忆（方案B：跨周期相似事件召回）实现完成
- 背景：讨论 Reports 是否引入 mem0/hymemory。结论——不引入通用记忆层（4G 服务器跑不下 Qdrant；与 event_versions 语义重叠；与长期方案D知识图谱冲突）。改做领域化的「跨周期召回」。
- 新增/改动：
  - 新增 `backend/app/services/event_memory.py`：`EventMemoryIndex`（numpy 内存余弦）、`load_index()`、`embed_texts()`、`persist_embeddings()`、`format_memory_block()`、`cluster_query_text()`、`event_doc_text()`、`embedding_tag()`。
  - 新增迁移 `b0c1d2e3_event_embedding`（down_revision=`a9b8c7_prefilter_reason`，即原 head）：news 加 `event_embedding REAL[]` + `event_embedding_model VARCHAR(64)`，部分索引 `ix_news_event_memory`。
  - `models/news.py` 加对应两列（导入 `REAL`，`ARRAY` 原已有）。
  - `config.py` 加 `EVENT_MEMORY_*` 7 个配置项。
  - `event_synthesizer.py`：SYSTEM_PROMPT 加「历史同类事件使用规则」（禁止把历史内容写进 title/summary/latest_change，只能影响 why_important/watch_next）；`_build_user_prompt` 加 memory_block 参数（放在报道列表之前）；`_find_candidate_clusters` SQL 加 `has_embedding` 计算列（需传 `:emb_tag`）；`_synthesize_one` 返回值由 bool 改为 `dict|None`；新增 `_recall_memory()` 与 `_refresh_event_embeddings()`。
  - 新增 `backend/scripts/backfill_event_embeddings.py`（冷启动回填，BATCH_SIZE=32）。
  - 新增 `backend/tests/test_event_memory.py`（20 用例）；`test_event_synthesizer.py` 三个用例加 `ms.EVENT_MEMORY_ENABLED=False` 隔离 + `_make_cluster` 加 `has_embedding`。
- **关键设计决策**：
  - **不用 pgvector**：候选量级千级，每轮 pipeline 只加载一次索引做 numpy 矩阵乘法，避免 4G 服务器装扩展 + ivfflat 索引常驻内存。
  - **相似度区间 [0.50, 0.67)**：上界对齐 `deduplicator.EMBEDDING_CLUSTER_THRESHOLD=0.67`（≥ 该值属同一事件，由去重器负责）。曾误设 0.93，已修正——放宽上界会把同一事件当成「历史规律」误导 LLM。
  - **`event_embedding_model` 标签 = `provider/model/dim`**：切换 EMBEDDING_PROVIDER 后旧向量自动失效，无需迁移、不会异构维度比对。本地默认标签为 `siliconflow/BAAI/bge-m3/1024`。
  - 每轮 pipeline 仅新增 2 次批量 Embedding 调用（召回 query + 回写事件包）；无实质更新且已有当前模型向量则跳过回写。
  - 全部失败路径静默降级为「无记忆」，不影响合成主流程。
- 验证：本地建 `backend/.venv`（已被 .gitignore 忽略）跑测试。`399 passed, 9 failed`，9 个失败经 `git stash` 基线比对**与改动前完全一致**（catalyst/digest_service/reports_api/us_data_fetcher 的既有失败），零回归。lint 全过。
- **已提交 + 已部署（2026-08-04 14:1x CST）**：commit `ffcd5e9` → push origin/main → 服务器 git pull → `alembic upgrade head`(`a9b8c7→b0c1d2e3` 成功) → `docker compose build web` + `up -d web` → 冷启动回填 `backfill_event_embeddings.py`(65 条历史事件向量全部落库,model=siliconflow/BAAI/bge-m3/1024)。
- **端到端验证通过**：一轮 pipeline 日志 `Event synthesis done: 3/3 clusters synthesized (memory recalled 9, embedded 3)` —— 召回命中 **100%**(3 簇均满额召回 top-3 历史事件)，3 个新事件包均写向量；`/api/v1/health` 返回 200。
- **初始观察(待一周再定)**：recall 满额偏高，可能 `EVENT_MEMORY_MIN_SIM=0.50` 偏松，后续若发现注入噪声可上调至 0.55~0.60。
- **遗留(非本次引入)**：pipeline 日志偶发 `Failed to mark URLs as seen in Redis: WRONGTYPE` —— Redis 旧键类型冲突，与方案B无关，未处理。

## 修复 CI 9 个预存后端测试失败（commit 2341a03，已 push + 部署）
- 背景：事件化重构后 9 个后端测试长期失败、CI 红。用户选择「全部修复（跨模块）」。结论：**9 个失败均为测试/数据问题，生产代码行为正确**，故只改测试与一处路由挂载，未改业务逻辑。
- 改动文件 + 根因：
  1. `backend/app/api/v1/router.py`：**重新启用 reports 路由**（commit 110bd10 事件化重构时整体暂停，注释「深度报告模块已暂停」）。挂载到 `v1_router`（公开可读），**不**放入需 API Key 的 `_protected`——因为 `/sync`、`/delete` 走自身 Bearer 令牌，放 _protected 会让 Node 同步脚本在生产需额外 API Key。list/detail 本就无鉴权（前端公开可读）。
  2. `tests/test_catalyst.py`：A 股行情链接已从富途切**雪球**（富途 A 股网页版下线），更新 4 处 URL 期望（futunn.com/stock/xxx → xueqiu.com/S/xxx）；`seed_news_data` 漏设 `created_at`，而 `_fetch_high_score_news` 按 `created_at` 当日过滤 → 返回 0，补 `created_at=datetime(2026,3,22,10,0,utc)`。
  3. `tests/test_digest_service.py`：`generate_digest` 无事件/无候选时按 PRD 跨简报对比生成 **no-LLM 简报并返回 ok**（仍落库+推送），非 skip；两处断言 `skip`→`ok`。
  4. `tests/test_us_data_fetcher.py`：`_tencent_us_code("A")` 返回 `usA.N` 正确（A=Agilent 在 NYSE 上市，核心列表内），修正期望值 `usA.OQ`→`usA.N`。
- 验证：本地 `.venv-test` 全量 `408 passed, 0 failed`（约 18 分钟）。push origin/main 成功，服务器 git pull + `docker compose up -d --build`；`/api/v1/reports/` 与 `/api/v1/health` 均 200。

## Reports / News 页面排版重设计（未提交）
- 用户要求（基于 Reports 页截图）：1) 去除右侧「栏目导航」模块；2) Reports 和 News 字号都放大；3) 导航:内容 = 1:9 分栏。
- 改动：
  - `pages/reports/index.vue`：模板删除整个 `pc-right-panel`（栏目导航）。`@media (min-width:1200px)` 新增 `page-layout`(flex)/`pc-sidebar`(flex:1, width:130px)/`container`(flex:9, max-width:none) 实现 1:9；digest 标题 21→24px、正文 14.5→16px、dc 系列 15→16-17px、研报卡标题 17→19px 等全量放大 ~10-15%。
  - `pages/index/index.vue`(News 页)：`@media (min-width:1200px)` 比例由 2:14:4 改为 1:9:3（导航 flex:2→1 / 内容 flex:14→9 / 右侧 TOP5 保留缩为 flex:3）；新闻标题 17→19px、摘要 15→16px、标签 13→14px、评分 15→17px 放大。
- lint 全过；未重新部署（用户仅回「好」，未要求 build/commit）。
- 注意：News 页右侧 TOP5 面板**未删除**（用户只说删 Reports 的「栏目导航」）。后续若也要统一去掉需另议。
