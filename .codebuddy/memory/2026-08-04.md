# 2026-08-04 工作日志

### 28. 影子 24h 复盘（修复后 00:35 UTC 08-03 → 01:30 08-04）
- 窗口 **857 条新闻**。REASON_DIST：None 710(83%)；官方/权威强制送评 59；重大事件强制送评 21；内容不完整/低价值 19；同事件有新事实(送评) ~50（均送评、非压缩）；C级硬信号不足 2；prefilter_score过低 2。
- **会丢弃 23 条(占 2.7%)**：评分分布 {5:11, 6:11, 7:1}。
  - 误杀(score≥7 被丢)=**1**：「特朗普：霍尔木兹海峡可能最迟明天重开」(富途,sc7)。根因：中文 NER 只匹配 公司/集团/央行/政府… 后缀，`特朗普`/`霍尔木兹海峡` 不被认作实体，且无数字/动作动词→`hard_signal_score=0`→被判低价值快讯。属实体/动作词表缺口，非规则缺陷。
  - 低分命中 22/23=**95.7% >80%** ✓；整体误杀率 1/857=**0.12%**。
  - 注：23 条丢弃中 11 条 sc5(明显低值)+11 条 sc6(展示阈值边界)+1 条 sc7。若把"不该丢"放宽到 sc≥6，则 12/23=52%——一半丢弃是边界 sc6。是否可接受取决于对 sc6 边界项的容忍度。
- **事件压缩(inherit)全窗口 0 触发**：无一条"同事件无新事实"决策。EVENT_SIM_THRESHOLD=0.85 过高，且到达的相似新闻几乎都带新事实→全送评。
- **信源 D 级限流 0 命中**：尚无信源达 D 门槛(display_pass_rate<3% 且样本≥200)，C 级仅 2 命中。
- **结论**：安全性已达标，可关影子；但**实际仅省 ~3% 评分 token，远低于设计 30–50% 目标**。三大省 token 杠杆(丢弃/信源D级/事件压缩)中仅"丢弃"生效(2.7%)，后两者几乎 dormant。要达目标需：① 降 `EVENT_SIM_THRESHOLD` 至 ~0.6–0.7 捕捉媒体跟稿；② 调低 D 级门槛或积累 30 天历史；③ 对边际 sc5–6 更激进(但会再增 sc6 边界丢弃)。
- 待用户决定：直接关影子(`PREFILTER_SHADOW_MODE=False`) 还是先调阈值提效。
- **用户决策（08-04）：继续影子**——保持 `PREFILTER_SHADOW_MODE=True` 不变，不关拦截、不调阈值，继续累积数据。下一步：等更长窗口让信源 D 级历史样本积累、事件压缩在更"脏"时段自然触发；后续再决定是关影子还是先提效。

### 29. 企微群机器人推送 Reports（已完成部署）
- 用户无企微管理员权限（腾讯员工），故走**群机器人 webhook**（无需 admin），非自建应用消息接口。Webhook：`qyapi.weixin.qq.com/cgi-bin/webhook/send?key=63de3...d43`（已配到服务器 `/home/Alphareader/.env` 的 `ALERT_WEBHOOK_URL`）。
- 新增 `backend/app/services/notifier.py::send_report()`（按行切分 ≤2000 字节，企微 text 单条上限 2048）；`briefing_service.py::build_wecom_summary()` 生成**无表格纯文本摘要**（企微 markdown 不支持表格、text 上限 2048 字节）——市场情绪+要闻+Tier S/X 重点，单条可发。`generate_briefing()` 现把 `wecom_summary` 写入 `meta` 持久化并随返回值带回；`_briefing_job`（工作日 09:00/16:00）生成后自动 `send_report`。
- 新增 `backend/scripts/push_latest_briefing.py`：从 `daily_briefings.meta.wecom_summary` 读取并立即推送（用于补推/验证），容器内 `docker compose run -e PYTHONPATH=/app web python scripts/push_latest_briefing.py`。
- 部署用 scp（未 git commit/未 push，因为规则禁止主动提交）：notifier/briefing_service/scheduler/llm_client + 新脚本 scp 到 43.136.86.36，`docker compose up -d --build web`。已实测 webhook 连通 + 推送 200 OK。
- 副作用：`ALERT_WEBHOOK_URL` 同时承载 pipeline 错误告警（send_alert）；若想报告与告警分离，后续可加 `REPORT_WEBHOOK_URL`。
- 顺带修复 `llm_client.stream_chat` 流式响应遇 503 时 `e.response.text` 抛 `ResponseNotRead` 致重试中断崩溃的 bug（改为 `await e.response.aread()` 安全读 body）。本次 DeepSeek 高峰 503 频发，修复后重试/兜底正常。

### 30. 推送目标纠正：应推 NewsDigest（四场），非 DailyBriefing
- **用户纠正**：要推的是「Reports 中的 Reports」= `news_digest` 阶段简报，一天四场 `morning`(早间)/`midday`(午间)/`evening`(傍晚)/`night`(夜间)，由 `digest_service.generate_digest` 生成、调度 08:30/12:15/18:15/00:00 触发。原先误推 `daily_briefing`（一天一份综合研报）是错的，已撤销。
- **链接要求**：推送正文末尾必须附原文链接 `https://www.alphareader.site/#/pages/briefing/detail?id=<digest_id>`（digest id 即前端 `pages/briefing/detail` 的 id）。新增 `config.SITE_BASE_URL`（默认 `https://www.alphareader.site`）。
- **实现**：`digest_service.build_wecom_digest_summary(structured, period_label, digest_id)` 生成无表格纯文本（period_summary + must_know≤5 + worth_watching≤5 + upcoming≤3 + 原文链接 + 免责），`_push_digest_to_wecom` 在 `generate_digest` 两个成功落库路径后 `await send_report(...)`；`send_report` 仍按行切分 ≤2000 字节（早间简报实测 2 条消息）。
- 手动脚本改为 `scripts/push_latest_digest.py`（`--date/--period`，读 news_digests.structured_content 推送）；旧 `push_latest_briefing.py` 已删除。
- 已 scp 部署（config/digest_service/briefing_service/scheduler + 新脚本）并 `docker compose up -d --build web`；实测早间 digest id=548 推送 200 OK、带原文链接。
