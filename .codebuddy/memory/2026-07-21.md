# 2026-07-21

## DeepSeek API 花费突增根因
- **主因（commit `6db3fb2`，7/14）**：评分/翻译 LLM 从**免费** SiliconFlow Qwen3-8B 切到**付费** DeepSeek-V4-flash（`LLM_MODEL`/`LLM_API_URL`=api.deepseek.com）。评分是最高频任务（pipeline 每15min、7:00–23:00，批20条、max_tokens=4096），此前为0成本。
- **叠加**：`run_pipeline` 每条 NEW 新闻走两次 LLM——Step3 `filter_news` 批量评分 + Step3.5 `_enrich_sentiment` 逐条情绪分析（pipeline.py:426）；英文还额外两阶段翻译。
- **调度器自愈（7/17）**：修复前调度器常因 stale 锁停摆→pipeline/digest/catalyst/briefing 全0调用；修复后恢复稳定执行，相对停摆期"暴增"（实属恢复）。
- 注：SiliconFlow 仍仅用于 Embedding 去重（免费）；digest/briefing 一直用 DeepSeek 但低频。
- 优化方向（已提议未执行）：②评分回流免费/低价模型；③查 `POST /api/v1/news/trigger` 是否被外部高频触发。

## 已执行：移除逐条情绪 LLM 调用（commit `1e8e2e7` 已在 origin/main，已部署 43.136.86.36）
- 用户确认"没必要调用情绪 llm"。删除了 `pipeline.py` 的 `_enrich_sentiment`（原 Step 3.5，每条 NEW 新闻逐条调 `analyze_sentiment_and_surprise_with_llm`，max_tokens=256）和死模块 `sentiment_pipeline.py`（428 行，全仓仅被该函数引用）。
- 影响：新新闻不再带 `sentiment_score/surprise_factor/catalyst_type/sentiment_entity/sentiment_reasoning`（落库为 NULL）；前端 `NewsCard.vue` 情绪徽标是 `v-if="sentiment_score != null"`，自动隐藏不报错；`CatalystTab.vue` 用的是 `catalyst_types`（复数，另一来源），不受影响。后端无其他代码引用这些字段，测试无依赖。
- 验证：容器 `sentiment_pipeline.py` 不存在、`_enrich_sentiment` 计数 0、health=200。这是省 DeepSeek 花费最大的一刀（此前每条新闻 1 次完整 LLM 调用）。
- 注：原优化①"合并进评分 prompt"未做，直接删除了；`ScoredNewsItem` 中情绪字段定义仍保留（可选、默认 None），无碍。

## Reports digest 时间避开 DeepSeek 峰谷定价（commit `9cfc543`，已部署）
- DeepSeek 7月中旬起峰谷定价：高峰时段（北京时间 9:00~12:00、14:00~18:00）所有计费项 2 倍价。
- 将 `digest_midday` 由 12:00→**12:15**、`digest_evening` 由 18:00→**18:15**（scheduler.py）。morning 08:30 / night 00:00 本就在峰段外，未动。已验证新容器 job 名含 12:15 / 18:15，health=200。
- 注意：仍有其他 LLM 任务落在高峰段（catalyst 08:45/15:50、briefing 09:00/16:00、VCP 16:10 等），若想进一步省 2 倍价可后续平移，但用户本次仅要求 Reports 午间/傍晚。
