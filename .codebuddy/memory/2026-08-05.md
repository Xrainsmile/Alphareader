# 2026-08-05

## 排版重设计 V2：修复布局崩溃并重新部署（commit 51d1c8a，已 push + 部署）
- **V1（98e6207）导致前端完全崩溃**：页面层 `.pc-sidebar` 的 `width:auto; flex:2 1 0` 等覆盖与 PcSidebar 组件自身 scoped 样式级联冲突，导致 News 页布局彻底乱掉（三列重叠、内容溢出）。
- **V2 修复策略**：回退三个文件到基线（2341a03），然后做**最小精确修改**，不再动已有 flex 结构：
  1. `PcSidebar.vue`：仅改一行 `width: 180px → 130px`（组件层统一控制）。
  2. `reports/index.vue` 模板：删除右侧「栏目导航」模块（~15行模板）。
  3. `reports/index.vue` 1200px 块（原为空）：仅加字号放大（dc-title 23px、正文16px、研报标题19px等），不动布局。
  4. `index/index.vue`(News) 1200px 块：`.pc-sidebar` 改为 `width:130px; flex:0 0 auto`（明确不与组件冲突）；`.container` 改 `flex:1` 撑满；右侧 TOP5 固定 `flex:0 0 280px`；加字号放大（标题19px、摘要16px等）。
- 验证：lint 零错误；`npm run build:h5` 成功；Lighthouse 服务器 `git pull`(98e6207→51d1c8a) + `docker compose up -d --build frontend` 成功；线上 https://www.alphareader.site/ 返回 200。
- **教训**：uni-app scoped CSS 下，组件内 `.pc-sidebar` 和页面内 `.pc-sidebar` 是不同 data-v 选择器，同时设置 flex/width 会产生不可预测的级联。正确做法：组件层控制侧栏尺寸，页面层只控制 container 和同级兄弟。
