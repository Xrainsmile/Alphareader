# 2026-08-05

## Reports / News 页排版重设计修复 + 部署（commit 98e6207，已 push + 部署）
- 昨日排版的预览 bug：Reports 页 `.page-layout` 只在 ≥1200px 才 `display:flex`，预览窗 <1200px 时导航从「左」变「上」堆叠，用户感知为「导航没了」；News 页侧栏用 `flex:1 1 0`+`min-width:0` 会塌成细条。
- 根因：页面层 `.pc-sidebar` flex 覆盖与 PcSidebar 组件自身样式（768px 设 `width:180px; flex-shrink:0; display:flex`）级联冲突。
- 修复（彻底消除冲突）：
  - `PcSidebar.vue`：导航宽度由组件统一固定 **130px**（原 180px），padding `24px 16px`→`24px 12px`。所有页一致。
  - 页面层不再覆盖 `.pc-sidebar` flex，只让 `.container { flex:1 }` 撑满 → 导航130px:内容≈**1:9**。
  - `reports/index.vue`：`.page-layout` 改为 **768px 起就 flex 左布局**（原仅1200px）；1200px 块移除 `.pc-sidebar` 覆盖。
  - `index/index.vue`：1200px 块移除 `.pc-sidebar` 覆盖，右侧 TOP5 由 `flex:3 1 0` 改为固定 `flex:0 0 280px`，内容 `flex:1`。
- 验证：`npm run build:h5` 本地构建成功（仅 svg 资源运行时解析警告，无碍）；Lighthouse 服务器 `git pull`(2341a03→98e6207) + `docker compose up -d --build frontend` 成功，`alpha-frontend`/`alpha-web` 均 Up，线上 `https://www.alphareader.site/` 返回 200。
- 注：News 页右侧 TOP5 面板**保留未删**（用户仅要求删 Reports 的「栏目导航」）。后续若也要去可另议。
