# 2026-08-06

## llm_news_filter.py 评分 Prompt 精简修订（按用户提供的精简修订版文档）
- 删除 Minervini SEPA / CAN SLIM 角色设定；删除"具名公司/具体数据/明确事件至少 6 分"保底，取消刻意抬分指令，恢复分数区分度。
- 旧闻改为渐进式时效降权：24h 内正常；1—7 天轻度降权；>7 天无新增最高 4 分；>30 天最高 2 分；旧事件首次披露关键数据按新增内容评。
- 新增来源可信度（匿名/未确认最高 7 分；自媒体/转述/主要重复已知事实最高 4 分）与重复新闻规则。
- 放宽 `why_it_matters`：点明盈利/估值/供需/政策/预期影响即可，禁补写原文没有的数据；删除全部 JSON 示例避免锚定；明确正文截断只评可见内容。
- 保留全部 JSON 字段未改名（零代码适配成本）。
- `_build_user_prompt()`：代码计算 `age_hours` 注入（不再让模型算时间差），顶部加"不可信数据"声明；新增 `ids` 参数供翻译阶段传入原始 id。
- 翻译两阶段改为用 `ScoredNewsItem.original_id` 对齐（新增该字段），`id` 可能不连续、禁止重新编号；`_parse_translate_response` 放宽越界检查接受不连续 id。
- 同步更新 `doc/llm_scoring_prompts.md`。
- 注意未提交/未部署；部署建议保持 `PREFILTER_SHADOW_MODE=True` 灰度观察评分分布。

## 修复「!」侧门访问验证弹窗被内容块遮挡
- 现象：GateButton 点 3 次弹出的密码弹窗展示在内容块下方。
- 根因：弹窗是 `GateButton` → `.gate-wrap` 的子元素，被 PcSidebar / Reports 页头所在的层叠上下文困住，`position:fixed + z-index:9999` 只在局部生效。
- 修复：`components/stocks/SandboxPasswordModal.vue` 模板外层包 `<teleport to="body">`，脱离页面层叠上下文进入根层叠上下文；`z-index` 9999 → 99999 兜底。
- 该组件被 GateButton / Stocks / SEPA 共用，一处改动全部受益。项目仅 H5 构建（`build:h5`），teleport 安全；CSS 变量定义在 `:root`，teleport 到 body 后仍继承。
- 验证：`npm run build:h5` 通过、lint 0 错误。
- 已提交 `b51a40f` 并部署（`docker compose up -d --build frontend`，alpha-frontend Started，https 200，产物含 pwd-overlay）。

## 后端 Prompt 修订上线 + 一次生产事故（同日）
- `349fdaa` 提交并部署 prompt 精简修订后**后端立即崩溃**：`TypeError: non-default argument 'score' follows default argument`。
- 原因：给 `ScoredNewsItem` 加 `original_id: int = 0` 时插在了非默认字段 `score` 之前，违反 dataclass 字段顺序规则。`9146285` 修复（移到 `tags` 之后），已部署恢复（alpha-web healthy，health 200）。
- **教训（重要）**：`python -m py_compile` 只做语法检查，**捕获不到 dataclass 字段顺序这类装饰器执行期错误**。以后改 dataclass/pydantic 模型后，必须额外做一次真实 import 或 exec 类定义验证，例如：
  `python -c "from app.services.llm_news_filter import ScoredNewsItem"`（容器内需 PYTHONPATH=/app），或本地用 ast 抽出该 ClassDef 后 exec。
- 当前生产调度确认正常：pipeline 25min，digest_morning 08:30 / digest_evening 18:30。
