# 2026-08-01

## ✅ 新闻断流修复部署完成 + 100M/天成本之谜闭环
- `55103a8`（年龄闸函数名修复）已部署：pipeline 全链路恢复，fetched 454→scored 111→stored 111，新闻实时入库。
- **100M/天真凶确认 = v4-flash 推理模型的隐藏 reasoning_tokens**：老 key（v4-flash）~100M/天 vs 新 key（deepseek-chat 非推理）实测 169 次/37万 tokens（~2,189/次），外推 ~0.7-1M/天，**降幅 ~99%**。老 key 已零调用（流量全是自己的，无泄漏）。
- 结论：评分/翻译长期用 `LLM_MODEL=deepseek-chat`（服务器 .env 已配），**不要切回 v4-flash**（推理模型对结构化评分任务纯浪费，且思考挤占 max_tokens 会截断 JSON 引发重试加倍）。
- 待办/观察：①用户可删除老 DeepSeek key；②V3 评分偏宽松（通过率 ~35%→~80%），若推荐流变杂把 LLM_SCORE_THRESHOLD 5→6；③deepseek-v4-flash 响应有 reasoning_content/reasoning_tokens，max_tokens 会被思考耗尽导致 content 为空（finish_reason=length）。
- **成本落地数字（用户实测）**：新 key + deepseek-chat 全天 ~¥0.31/天（~¥9-10/月），对比老 key 100M/天的数千元级月成本降三个数量级。**成本问题已闭环，无需再优化。**

## 本周重大变更汇总（7-30~8-1，均已上线）
- P0-P3 review 修复 13 提交：丢失链/调度器锁状态机/admin鉴权+限流+fail-fast/迁移补齐/ts_code归一化/时区/to_thread/seen_urls ZSET/news_cleanup/异常文案/token化/分层。
- 评分精简：arXiv 停用、年龄闸(>24h 本地预杀)、预览 800→400、翻译仅≥6、usage 用量日志。
- 运维事实：news_cleanup 每日 03:30 清 7 天前旧闻（首跑 79,722→4,910 行属预期）；pipeline_runs 统计表被截到 7-30（无业务影响）；DeepSeek 新 key 只有 chat/reasoner 两模型（v4-flash 权限有激活延迟）。

## ✅ 方案A「事件中心化」实现完成（本地，未部署）
- 背景：用户提出 news 模块本质是文章 feed，在大模型时代信息粒度错了（用户要"事件"不是"文章"）。四方案 A事件中心化/B对话式RAG/C个性化简报/D知识图谱，用户拍板先做 A，并明确看好 D（已存长期记忆）。
- 实现（feed 从"文章列表"变"事件卡片"，复用现有 related_to_id 聚合）：
  1. 迁移 `v4w5x6y7z8a9`（接链尾 u3v4w5x6y7z8）：news 加 event_title/event_summary/event_article_count 三列（仅写聚合根）。
  2. 新服务 `app/services/event_synthesizer.py`：扫描窗口内有新关联报道的事件簇（SQL jsonb_agg，PG only），增量判断（报道总数>event_article_count 才重合成），每簇 1 次 LLM（deepseek-chat，thinking disabled，temperature 0.2，max_tokens 512，失败重试1次）合成事件标题+综述回写聚合根。
  3. pipeline Step 7 挂载（非关键路径，失败仅记日志），summary 增 events_synthesized。
  4. config 四项：EVENT_SYNTH_ENABLED=True / WINDOW_HOURS=12 / MAX_EVENTS=10 / MIN_SOURCES=2。
  5. API：list_news + hot-topics 响应均补 event_title/event_summary。
  6. 前端 NewsCard.vue：displayTitle=event_title||title，displaySummary=event_summary||ai_summary（搜索高亮模式除外）。零新样式。
- 测试：新增 tests/test_event_synthesizer.py 12 用例全过；全量 292 passed + 6 failed（catalyst futu URL/us_data_fetcher，**stash 验证为既有失败，与本次无关**）。
- **部署待办**：上线前须先跑 `alembic upgrade head`（新迁移 v4w5x6y7z8a9），再 `--build web`（后端+模型变更）和 `--build frontend`（NewsCard 变更）。
