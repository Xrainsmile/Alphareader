# 2026-07-31

## ✅ P0-P3 + 评分精简全部部署完成（13 提交，43.136.86.36）
- 服务器 git 历史与重写后的 origin/main 分叉 → `git reset --hard origin/main` 对齐（a23f598）。
- **迁移被并行会话重写为更优版**：m0x1 补 5 表 + u3v4w5x6y7z8 补 related_to_id/market 索引，全 `IF NOT EXISTS` 幂等；原 `213dbaa` 提交已消失于 rebase。prod 迁移 `t2u3v4w5x6y7 → u3v4w5x6y7z8` 成功（related_to_id prod 已存在，幂等跳过）。
- 验证全绿：4 容器 healthy；health ok（postgres error 系重建瞬态）；调度器 Digest 12:15/18:15 + News Cleanup 03:30 已加载；admin 端点无 key 401；seen_urls 已 zset（新 pipeline 已跑）；arXiv 0 抓取；nginx 限流生效；DB 79,722 条新闻完好。
- 注意：`LLM usage` 用量日志部署后 15 分钟未出现——可能 DeepSeek 响应未带 usage 字段或窗口太短，观察期重点确认；若无 usage 需改用字符数估算或响应头。
- DeepSeek key 轮换（用户手动）：sed 改 .env 的 `DEEPSEEK_API_KEY` 一行 + `docker compose up -d web`。LLM_API_KEY 不存在属正常（AliasChoices 回落 DEEPSEEK_API_KEY）。
- 观察计划：轮换后 24h 看新 key 用量。预期 ~5-7M/天（评分精简后更低）；若仍 100M 级 → 查 LLM usage 日志定位场景/verifying reasoning tokens。

## 新闻断流事故（7-31 午间/傍晚 digest 空）根因与修复
- **根因**：年龄闸调用 `_mark_urls_seen`，但 rebase 时函数被改名为 `_mark_urls_as_seen` → 每轮 pipeline 在 Step2.5 NameError 崩溃，after_score/stored 全 0（部署后 ~22h 断流）。测试未覆盖 run_pipeline 年龄闸路径所以回归没抓到。**修复 `55103a8` 已推送**（一行改名），部署被用户拦下待执行。
- 次要发现①：新 DeepSeek key 初期调 v4-flash 报 Model Not Exist（新账户权限激活延迟），后实测 200 可用。期间我在服务器 .env 加了 `LLM_MODEL=deepseek-chat` 兜底（仍在生效）。
- 次要发现②：**v4-flash 是推理模型**（响应含 reasoning_content，usage 含 reasoning_tokens；max_tokens=10 全被思考吃掉致 content 为空）——隐藏推理 token 计费极可能是 100M/天真凶，且推理挤占 max_tokens 会截断 JSON 输出引发重试加倍。**建议评分/翻译长期用 deepseek-chat（非推理）**。
- 次要发现③：news_cleanup 03:30 首跑，删除 published_at>7 天旧闻 ~75K 行（79,722→4,910），属预期 7 天留存；pipeline_runs 被截到 7-30 16:15（疑并行会话清理逻辑，仅统计表无业务影响）。
- 待办：用户执行部署（git reset --hard origin/main + rebuild web）；观察 24h 新 key 用量；决定是否长期保留 LLM_MODEL=deepseek-chat。
